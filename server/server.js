/**
 * Bridge receiver entry for production (Render, Railway, VPS).
 * GitHub Pages cannot run this file — deploy the `server/` folder separately.
 *
 * Build first: `npm run build` → then `node server.js` or `npm start`
 */
import './dist/index.js'
